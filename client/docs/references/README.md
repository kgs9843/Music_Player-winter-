# references/

외부 문서(Context7 등)에서 확인한 내용을 **레포 안에 짧게 고정**합니다.

## 문서 목록

- `threejs-audio-context-dispose-2026-04-25.md`: three.js(WebAudio) dispose 시 AudioContext `close()` 주의 + `suspend/resume` 권장
- `context7-mcp-troubleshooting.md`: Context7 MCP가 안 될 때 원인/해결 방법(이 레포 환경 기준)

## UI 스크린샷

- 화면별 캡처·GitHub raw URL·갱신 명령(`npm run test:e2e:store-shots`, `playwright.config` 390×844·DPR 2): [`screenshot/README.md`](screenshot/README.md)
- (기존) Playwright CLI로 남긴 예시: [`screenshots/`](screenshots/)

## 원칙

- “링크만” 남기지 말고 **결론 3~7줄**을 같이 남깁니다.
- 반드시 **조회 날짜**와 **적용 대상 버전**을 적습니다.
- 같은 주제가 반복되면 문서 1개로 합치고 최신 내용을 상단에 추가합니다.
